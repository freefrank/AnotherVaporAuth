# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# .env is NOT in git but IS carried to the Windows mirror by the `s` rsync
# (same as app/android/key.properties) — the WSL tree stays the only source.

# SMTP password for hi@dotslash.pro, used by posts/zh/recruit/send_beta_*.py
# to mail beta redeem codes through Stalwart (mx.deployr.ca:587).
# Real value lives in ownCloud/Deployr/mailserver/CREDENTIALS.md, row
# "hi (dotSlash)".
SMTP_PASS=
